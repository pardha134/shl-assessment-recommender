# Ultra-lightweight deployment requirements
# Total size: ~500 MB (vs 8.6 GB with sentence-transformers)

# Core API (essential)
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1

# LLM and RAG (required for recommendations)
langchain==0.1.10
langchain-openai==0.0.8
langchain-community==0.0.24
openai==1.12.0

# Vector Store (lightweight)
faiss-cpu==1.8.0
numpy==1.26.4

# Configuration
python-dotenv==1.0.1

# Note: sentence-transformers REMOVED (saves ~4 GB)
# Embeddings are pre-computed in vector_store/shl_faiss/
# No runtime embedding generation needed
